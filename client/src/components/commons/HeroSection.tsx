import {Hero} from "@/components/ui/hero.tsx";

const HeroSection = () => {
    return (
        <div className="ml-50 mr-50 mt-20">
            <Hero />
        </div>
    );
};
export default HeroSection;
